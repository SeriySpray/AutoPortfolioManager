# 📚 База Знань: Кількісний Трейдинг (Quantitative Trading & Math Models)
*Детальний аналіз та конспект лекції "Intro to Quant Trading" (50 хв)*

---

## 1. Фундаментальне визначення Quant Trading

Кількісний трейдинг (Quant Trading) визначається двома нерозривними компонентами:
$$\text{Quant Trading System} = \text{Model (Edge Creation)} + \text{Strategy (Execution)}$$

1. **Створення статистичної переваги (Statistical Edge)** — знаходження стійкої нерівності математичного очікування $E[X] > 0$ на базі даних.
2. **Виконання (Execution)** — перетворення прогнозів моделі в ордери на біржі з контролем ризику, транзакційних витрат та оптимізацією коефіцієнта Шарпа (Sharpe Ratio).

> **Ключовий закон:** Навіть ідеальна модель з позитивним математичним очікуванням буде збитковою при неправильному виконанні (наприклад, завищений розмір позиції або ігнорування спреду/комісій).

---

## 2. Математичне очікування (Expected Value, EV) та чому Win Rate оманливий

Статистична перевага квантів вимірюється не відсотком виграшних угод (Win Rate), а **математичним очікуванням чистого P&L на угоду**:

$$E[X] = \sum_{i} P(x_i) \cdot x_i = P(\text{Win}) \cdot W - P(\text{Loss}) \cdot L$$

де:
- $P(\text{Win}), P(\text{Loss})$ — ймовірності прибутку та збитку;
- $W, L$ — середній розмір прибутку та збитку після врахування всіх комісій та проковзувань (Net P&L).

### Ілюстративні парадокси:
1. **Високий Win Rate, але від'ємне EV (Збиткова система):**
   - 55% виграш $1.00, 45% програш $1.25.
   - $E[X] = 0.55 \times 1.00 + 0.45 \times (-1.25) = 0.55 - 0.5625 = -0.0125$ (-1.25 цента за угоду).
   - *Висновок:* При 55% точності капітал неминуче зливається.
2. **Низький Win Rate, але високе EV (Прибуткова система):**
   - 25% виграш $35.00, 75% програш $1.00.
   - $E[X] = 0.25 \times 35.00 + 0.75 \times (-1.00) = 8.75 - 0.75 = +8.00$.
3. **Квантовий режим "Small Edge, High Volume":**
   - Win Rate: 51%–53%, $E[X] \approx +0.01\dots+0.05$.
   - За рахунок закону великих чисел та тисяч угод крива капіталу зростає плавно та монотонно.

---

## 3. Ризик та коефіцієнт Шарпа (Sharpe Ratio)

Ризик у квантових фінансах формалізується як стандартне відхилення дохідності $\sigma(R)$ (волатильність P&L).

### Формула коефіцієнта Шарпа:
$$\text{Sharpe} = \frac{E[R] - R_f}{\sigma(R)}$$

- Для внутрішньоденних (intraday) стратегій безризикова ставка $R_f \approx 0$:
  $$\text{Sharpe}_{\text{intraday}} = \frac{E[R]}{\sigma(R)}$$

### Чому високий Sharpe — це головна мета:
- **Зв'язок з кредитним плечем (Leverage):** Чим вищий Sharpe, тим стабільніша дохідність (менша $\sigma$), що дозволяє безпечно масштабувати капітал плечем без ризику ліквідації.
- Стратегії з низьким Sharpe мають глибокі просідання (Drawdowns), і будь-яке плече веде до банкрутства.

---

## 4. Логарифмічна дохідність (Log Returns) проти простої (Simple Returns)

### Проблема простої дохідності (Асиметрія):
$$R_t = \frac{P_t - P_{t-1}}{P_{t-1}}$$
Якщо ціна зросла на $20, а потім впала на $20 ($100 \rightarrow $120 \rightarrow $100):
- Ріст: $+20.0\%$
- Падіння: $-16.67\%$
- *Несиметричність:* ускладнює роботу моделей машинного навчання.

### Переваги логарифмічної дохідності:
$$r_t = \ln\left(\frac{P_t}{P_{t-1}}\right) = \ln(P_t) - \ln(P_{t-1})$$

1. **Симетричність:** $100 \rightarrow 120 \implies +18.23\%$; $120 \rightarrow 100 \implies -18.23\%$.
2. **Адитивність у часі (Time Additivity):**
   $$r_{0 \to T} = \sum_{t=1}^T r_t = \ln(P_T) - \ln(P_0)$$

---

## 5. Типи моделей та архітектура машинного навчання

### Структура моделі:
$$\hat{y} = f(X)$$
- **Вхід ($X$ / Features):** лаги часового ряду ($x_{t-1}, x_{t-2}, \dots$), параметри книги ордерів.
- **Ціль ($\hat{y}$ / Target):**
  - *Регресія:* передбачення $\Delta P$ (price delta) або майбутньої лог-дохідності $r_{t+h}$.
  - *Класифікація:* категорія (UP / DOWN) та ймовірність $P(\text{UP})$.

### Принцип Бритви Оккама (Occam's Razor):
- Чим більше ознак і параметрів у моделі (наприклад, глибокі нейромережі), тим вищий ризик **перенавчання на ринковому шумі (Overfitting)**.
- Прості лінійні / одномірні моделі (Univariate AR(1)) узагальнюють краще, обчислюються за 3–5 тактів процесора (CPU cycles) і мають повну інтерпретованість.

---

## 6. Авторегресія AR(1) та дві базові поведінки ринку

Модель одномірної лінійної авторегресії:
$$\hat{y}_t = w \cdot x_{t-1} + b$$

де $x_{t-1}$ — попередня лог-дохідність (лаг), $w$ — вага, $b$ — зсув (bias).

### 1. Повернення до середнього (Mean Reversion):
- **Ознака:** Вага $w < 0$.
- **Механіка:** Від'ємна вага змінює знак вхідного сигналу на протилежний (працює як "пружина" або гумка, що повертає ціну до центру).
- Характерно для ультракоротких таймфреймів (секунди, хвилини).

### 2. Імпульс / Тренд (Momentum):
- **Ознака:** Вага $w > 0$.
- **Механіка:** Додатна вага зберігає та підсилює поточний напрямок руху ($+ \to +$, $- \to -$).
- Характерно для середньострокових таймфреймів (години, дні).

---

## 7. Оптимізація параметрів (OLS vs Gradient Descent)

### 1. Аналітичний розв'язок (OLS - Ordinary Least Squares):
$$\mathbf{w} = (\mathbf{X}^T \mathbf{X})^{-1} \mathbf{X}^T \mathbf{y}$$
- *Плюси:* точний глобальний мінімум за один крок (1 рядок коду).
- *Мінуси:* обчислювальна складність $O(N D^2 + D^3)$, не масштабується на гігабайти/терабайти тікових даних у пам'яті.

### 2. Градієнтний спуск (Gradient Descent):
$$\mathbf{w} \leftarrow \mathbf{w} - \eta \cdot \nabla_{\mathbf{w}} L(\mathbf{w})$$
- $\eta$ (learning rate) — розмір кроку.
- $\nabla L$ — градієнт функції втрат.
- **Опуклі функції (Convex Loss):** один глобальний мінімум (MSE, L2).
- **Неопуклі користувацькі функції (Non-Convex Loss):** оптимізація під Sharpe Ratio, прибутковість з урахуванням просідання; оптимізатор шукає локальні мінімуми, стійкі до шуму.

---

## 8. Мікроструктура ринку та Книга Ордерів (Order Book)

- **Книга ордерів (LOB):** Bids (лімітні покупки) та Asks (лімітні продажі).
- **Спред:** $\text{Spread} = P_{\text{best\_ask}} - P_{\text{best\_bid}}$.
- **Середня ціна (Mid Price):**
  $$P_{\text{mid}} = \frac{P_{\text{best\_ask}} + P_{\text{best\_bid}}}{2}$$
- **Ефект Bid-Ask Bounce:** Останні ціни угод (Last Traded Price) хаотично скачуть між Bid та Ask. Моделювання на базі $P_{\text{mid}}$ усуває цей штучний шум.
- **Проковзування (Slippage / Walking the Book):** Великий маркет-ордер "з'їдає" кілька цінових рівнів, погіршуючи середню ціну виконання.

---

## 9. Стратегії Taker (Market Taking)

Тейкер використовує маркет-ордери (гарантоване виконання, платить комісію тейкера та спред). Застосовується на горизонтах від годин до днів.

### Таймінг входів:
1. **Time-based:** відкриття/закриття на кожному фіксованому кроці $\Delta t$.
   - *Frequency Scaling:* прогноз будується на 12 годин, але позиція коригується невеликими порціями щогодини (24 угоди на добу замість 2), максимізуючи використання невеликої переваги ($+EV$).
2. **Predicate-based (Фільтри):** вхід лише при перевищенні порогу сили сигналу:
   $$|\hat{y}| \ge \theta_{\text{threshold}} \quad \text{або} \quad P(\text{UP}) \ge 60\%$$

### Розрахунок розміру позиції (Position Sizing):
1. **Constant:** $S = \text{sign}(\hat{y}) \cdot S_{\text{fixed}}$.
2. **Кусково-лінійний (HardTanh):**
   $$S = S_{\max} \cdot \text{HardTanh}(\hat{y})$$
3. **Нелінійний гладкий ($\tanh$):**
   $$S = S_{\max} \cdot \tanh(k \cdot \hat{y})$$
   - Гладке, обмежене $[-S_{\max}, S_{\max}]$, диференційовне для градієнтної оптимізації.

---

## 10. Стратегії Maker (Market Making)

Мейкер виставляє лімітні ордери (додає ліквідність, заробляє спред + ребейки біржі). Застосовується на таймфреймах секунд та мілісекунд.

### Ключові концепції:
1. **Несприятливий відбір (Adverse Selection):** якщо спред занадто вузький, лімітний ордер виконується тоді, коли ринок агресивно валиться проти нього.
2. **Зсув котирувань (Quote Skewing / Biasing):**
   - Якщо модель прогнозує $\Delta P_{\text{mid}} > 0$ (ріст):
     $$\text{Bid} \uparrow, \quad \text{Ask} \uparrow \quad (\text{спред зберігається})$$
   - Це збільшує ймовірність виконання Bid і накопичення лонг-позиції перед ростом ціни.
   - Якщо модель прогнозує $\Delta P_{\text{mid}} < 0$ (падіння):
     $$\text{Bid} \downarrow, \quad \text{Ask} \downarrow \quad (\text{накопичення шорт-позиції})$$

---

## 11. Мапа фундаментальних навичок квант-інженера

```
                          ┌───────────────────────────┐
                          │    QUANTITATIVE TRADER    │
                          └─────────────┬─────────────┘
                                        │
         ┌──────────────────┬───────────┴───────────┬──────────────────┐
         │                  │                       │                  │
┌────────┴─────────┐ ┌──────┴─────────┐ ┌───────────┴──────────┐ ┌─────┴──────────┐
│   MATHEMATICS    │ │  ECONOMETRICS  │ │   MACHINE LEARNING   │ │   PROGRAMMING    │
├──────────────────┤ ├────────────────┤ ├──────────────────────┤ ├────────────────┤
│• Linear Algebra  │ │• Time Series   │ │• Supervised Learning │ │• Python / C++  │
│• Multivar Calc   │ │• AR(p) Models  │ │• Gradient Descent    │ │• Low Latency   │
│• Probability/Stat│ │• Stationarity  │ │• Loss Engineering    │ │• 24/7 Systems  │
│• Convex Optim    │ │• Cointegration │ │• Occam's Razor       │ │• Backtest APIs │
└──────────────────┘ └────────────────┘ └──────────────────────┘ └────────────────┘
```
